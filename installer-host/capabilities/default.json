{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "installer-default",
  "description": "Permissions for the standalone CSS installer window.",
  "windows": ["installer"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
